#include <stdio.h>

int main()
{
    char frase[80];
    int i = 0,j,tam;

    printf("Digite uma frase:")
    gets(frase);
    tam = strlen(frase)

    for (i = 0;i  < tam;i++)
        if (frase[i] == ' ' )
        {
            n == i;

        }



}
